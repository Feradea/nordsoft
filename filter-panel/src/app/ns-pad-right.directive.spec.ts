import { NsPadRightDirective } from './ns-pad-right.directive';

describe('NsPadRightDirective', () => {
  it('should create an instance', () => {
    const directive = new NsPadRightDirective();
    expect(directive).toBeTruthy();
  });
});
