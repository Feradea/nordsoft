import { NsPadLeftDirective } from './ns-pad-left.directive';

describe('NsPadLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new NsPadLeftDirective();
    expect(directive).toBeTruthy();
  });
});
